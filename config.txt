server {
    listen 80;
    server_name safemint.vip;
    return 301 https://safemint.vip$request_uri;
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name safemint.vip;

    root /www/wwwroot/safemint/dist;
    index index.html;

    ssl_certificate /etc/letsencrypt/live/safemint.vip/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/safemint.vip/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        try_files $uri /index.html;
    }

    location ~ /\.ht {
        deny all;
    }
}


server {
    server_name app.bdcstack.com;

    location / {
        proxy_pass http://localhost:1735;   # Node.js app on port 1735
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/app.bdcstack.com/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/app.bdcstack.com/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot

}
server {
    if ($host = app.bdcstack.com) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    listen 80;
    server_name app.bdcstack.com;
    return 404; # managed by Certbot


}







server {
    listen 80;
    server_name bdcstack.com;

    location / {
        proxy_pass http://localhost:1737;   # Node.js app port
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

server {
    listen 80;
    server_name viprealtyworld.com

    location / {
        proxy_pass http://localhost:1741;   # Node.js app port
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

ln -s /etc/nginx/sites-available/safemint /etc/nginx/sites-enabled/
ln -s /etc/nginx/sites-available/bdcstack.com /etc/nginx/sites-enabled/
ln -s /etc/nginx/sites-available/viprealtyworld /etc/nginx/sites-enabled/
nginx -t
systemctl reload nginx
certbot --nginx -d viprealtyworld.com
certbot --nginx -d app.felix9.io
ln -s /etc/nginx/sites-available/felix-landing /etc/nginx/sites-enabled/
ln -s /etc/nginx/sites-available/app.bdcstack.com /etc/nginx/sites-enabled/




server {
    if ($host = admin.robomine.live) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    listen 80;
    server_name admin.robomine.live;

    return 301 https://$host$request_uri;  # Redirect HTTP to HTTPS


}

server {
    listen 443 ssl http2;
    server_name admin.robomine.live;
    ssl_certificate /etc/letsencrypt/live/admin.robomine.live/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/admin.robomine.live/privkey.pem; # managed by Certbot
    include /et
    
server {
    listen 443 ssl http2;
    server_name admin.robomine.live;
    ssl_certificate /etc/letsencrypt/live/admin.robomine.live/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/admin.robomine.live/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot

    root /www/wwwroot/ROBOMINE/robomine/admin/dist;
    index index.html;

    location / {
        try_files $uri /index.html;
    }

    location ~ /\.ht {
        deny all;
    }


change domain - bdcstack.com
dir - /www/wwwroot/bdc-stack/bdc/dist
 remove ssl geertate by using certbot 


server {
    listen 80;
    server_name bdcstack.com ;

    root /www/wwwroot/bdc-stack/bdc/dist;
    index index.html;

    location / {
        try_files $uri /index.html;
    }

    location ~ /\.ht {
        deny all;
    }
}


server {
    listen 80;
    server_name felix9.io;

    # Redirect to HTTPS
    return 301 https://$host$request_uri;
}

# HTTPS server
server {
    listen 443 ssl http2;
    server_name app.felix9.io;

    root /www/wwwroot/felix9/dist;
    index index.html;

    # Single Page App routing
    location / {
        try_files $uri /index.html;
    }

    # Deny hidden files
    location ~ /\.ht {
        deny all;
    }

    # SSL certificates from Let's Encrypt
    ssl_certificate /etc/letsencrypt/live/fe